language: python
env:
  matrix:
    - PYTHON: "C:\\Python35-x64"
    
before_install:
  
- pip install requirements.txt
  
script:
- "%PYTHON%\\python.exe -m pytest -v --ignore=tests/gui/"
os:
  - linux
